package utils

const (
	TCP            = "tcp"
	SERVER_IP      = "0.0.0.0"
	SERVER_PORT    = "4567"
	SERVER_ADDRESS = SERVER_IP + ":" + SERVER_PORT // fmt.Sprintf doesn't work here
	VERSION_ONE    = "1.0"
)
